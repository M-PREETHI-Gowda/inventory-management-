import initialState from '../constants/initialState';

function initialReducre(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
export default initialReducre;
